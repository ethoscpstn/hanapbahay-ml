# Pre-compiled wheels for Windows (no build tools required)
# Use this if requirements.txt fails due to missing Visual Studio

fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.0
python-multipart==0.0.12

# Use pip to auto-download pre-compiled wheels
scikit-learn>=1.3.0
pandas>=2.0.0
numpy>=1.24.0
joblib>=1.3.0

# Database connectors (optional - only if training locally)
# Uncomment the one you need:
# psycopg2-binary>=2.9.0  # PostgreSQL
# mysql-connector-python>=8.0.0  # MySQL

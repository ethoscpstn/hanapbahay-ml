services:
  - type: web
    name: hanapbahay-ml
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: HANAPBAHAY_API_KEY
        value: hanapbahay_ml_prod_secure_2024
